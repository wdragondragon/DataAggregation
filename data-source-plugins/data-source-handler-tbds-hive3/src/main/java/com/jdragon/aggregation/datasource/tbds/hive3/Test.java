package com.jdragon.aggregation.datasource.tbds.hive3;

import java.util.concurrent.TimeUnit;

public class Test {
    public static void main(String[] args) {
        String s = String.valueOf(TimeUnit.SECONDS.toMillis(60));
        System.out.println(s);
    }
}
