# 交接记录：SortMerge 局部乱序缓冲与 Spill 护栏

## 本轮完成内容

### 核心能力
- 已在 `AdaptiveMergeConfig` 中补齐局部乱序与 spill 护栏配置：
  - `localDisorderEnabled`
  - `localDisorderMaxGroups`
  - `localDisorderMaxMemoryMB`
  - `maxSpillBytesMB`
  - `minFreeDiskMB`
  - `preferOrderedQuery`
  - `onOrderViolation = RECOVER_LOCAL`
- `OrderedSourceCursor` 已支持 source 侧有界局部乱序缓冲。
- `AdaptiveMergeCoordinator` 已支持 `RECOVER_LOCAL`：
  - 不再一遇到残余乱序就直接全局 `bucketMode`
  - 改为只 spill 受污染前缀
  - 维护 `maxObservedKeyBySource`
  - 继续通过 `bucketUpperBound` 约束受污染前缀路由
- 已新增共享 `SpillGuard` 与 `SpillLimitExceededException`。
- `OverflowBucketStore`、`PartitionedSpillStore`、fusion executor、consistency executor 都已接入同一份 spill 配额控制。
- `SortMergeStats` 已补齐：
  - `localReorderedGroupCount`
  - `localMergedDuplicateGroupCount`
  - `orderRecoveryCount`
  - `spillBytes`
  - `spillGuardTriggered`
  - `spillGuardReason`

### core 测试层
- `AdaptiveMergeCoordinatorTest` 已补充：
  - source 侧局部乱序吸收
  - `RECOVER_LOCAL`
  - `BUCKET`
  - `FAIL`
  - `bucketUpperBound` 前缀污染路由
- 已新增 `SpillGuardTest`

### data-mock 测试支撑层
- `AdaptiveSortMergeTestSupport` 已补齐：
  - `ScenarioOptions`
  - 可选 `preferOrderedQuery`
  - 可选自定义 `AdaptiveMergeConfig` 覆盖
  - 稀疏局部倒退 query 构造
  - consistency/fusion 新统计字段透出
  - fusion 错误消息透出
- `AdaptiveSortMergeMysqlIntegrationTest` 已新增两个场景：
  - 稀疏局部倒退但应由 source 缓冲吸收，不应过早进入 bucket
  - spill 配额过小，应快速失败

## 本轮未做内容
- 本轮未运行新的 MySQL 集成验证。
- 本轮未重新跑 data-mock benchmark。
- 本轮未继续扩展 benchmark 报告中的统计列。

## 当前明确约束
- 当前工作环境还没有 MySQL，因此本轮只完成代码与交接，不做集成验证。
- 这轮没有继续处理 hybrid 后全局输出顺序问题。

## 明天接手建议

### 优先验证顺序
1. 先跑 core 层测试，确认本轮代码改动未引入基础回归。
2. 再跑 `AdaptiveSortMergeMysqlIntegrationTest`：
   - 默认小样本场景
   - 稀疏局部倒退场景
   - spill 护栏快速失败场景
3. 如果 MySQL 环境稳定，再跑 benchmark 场景观察：
   - `localReorderedGroupCount`
   - `orderRecoveryCount`
   - `spillBytes`
   - `mergeSpilledKeyCount`

### 重点关注点
- 稀疏局部倒退场景下：
  - `executionEngine` 应保持在 `sortmerge` 或 `hybrid`
  - 不应因为一次局部倒退直接进入全局 `bucket`
  - `orderRecoveryCount` 在 source 缓冲能吸收时应保持为 0
- spill 护栏场景下：
  - consistency 应返回失败状态，并带有 `spillGuardTriggered`
  - fusion 应快速失败，并透出 `Spill limit exceeded` 之类的错误消息

### 如果验证失败，优先排查
- `AdaptiveSortMergeTestSupport` 的 query 构造是否被数据源层重新排序覆盖
- `preferOrderedQuery=false` 是否在 sortmerge reader 路径里生效
- 残余乱序是否真的进入了 `RECOVER_LOCAL`，而不是被旧逻辑提前切到 `BUCKET`
- spill guard 是否在 rebalance 子层也复用了同一份配额
